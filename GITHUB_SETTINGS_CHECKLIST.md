# GitHub setup for version 1.0.0

Upload the CONTENTS of this source directory to the existing repository root, including `.github`, `.gitignore` and `.gitattributes`. Keep `src/`, `scripts/`, `tests/`, `configs/`, `results/` and `figures/` as ordinary directories. Do not upload this source ZIP as a release asset. Preserve the existing MIT license and earlier history.

Suggested About description: Scalar stress-gated flocking coordination for CPES: source, analytic tests, component ablations, sensitivity and five-topology reproducibility trials.

Suggested topics: `flocking`, `distributed-control`, `cyber-physical-systems`, `energy-systems`, `smart-grid`, `multi-agent-systems`, `consensus`, `graph-theory`, `volt-var-control`, `energy-storage`, `ev-charging`, `heat-pumps`, `simulation`, `reproducibility`, `research-software`, `open-science`.

Website before article publication: https://github.com/kovariati/Numerical-Illustration-for-Flocking-Based-Coordination-of-Heterogeneous-Units-in-CPES. Set it to the real article DOI after publication. Social preview: `figures/social-preview.png`.

After source and result checks pass on the uploaded commit, create tag v1.0.0 and use `RELEASE_NOTES.md`. Review Issues, branch protection, default branch, and Actions settings as appropriate for the existing repository. A public tag, CI result, article DOI or archive DOI must not be claimed before it exists. Zenodo and Software Heritage are optional later archival steps; no fabricated deposit metadata is supplied.
